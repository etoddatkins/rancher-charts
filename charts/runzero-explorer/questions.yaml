---
questions:
  - variable: image.repository
    default: localhost:5000/explorer
    description: Local private docker image repository
    label:
    required: true
  - variable: image.tag
    default:
    description: Image tag should be a code or abbreviation of the Explorer's runZero Organization
    label:
    required: true
